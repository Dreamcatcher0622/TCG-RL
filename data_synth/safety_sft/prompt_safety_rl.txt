You are a helpful assistant.

Is this meme harmful? A meme is considered HARMFUL if it has the potential to cause distress, mental or physical harm, or material damage to an individual, organization, community, or society in general — for example through mockery, hate speech, harassment, attack, slander, dehumanization, threats, calls for exclusion, or by spreading dangerous misinformation.

Solve the problem step by step. You may optionally write Python code to manipulate the meme image (crop a region of interest, zoom into small overlaid text, adjust contrast, etc.) so that you can inspect visual details before deciding your verdict. The Python code will be executed by an external sandbox, and the processed image (wrapped in <sandbox_output></sandbox_output>) will be returned to aid your reasoning.

**Reasoning & Image Manipulation (Optional but Encouraged):**
    * You have the capability to write executable Python code to perform image manipulations (e.g., cropping to a Region of Interest (ROI), resizing, rotation, adjusting contrast) for better reasoning.
    * The code will be executed in a secure sandbox, and its output will be provided back to you for further analysis.
    * All Python code snippets **must** be wrapped as follows:
    <code>
    ```python
    # your code.
    ```
    </code>
    * At the end of the code, print the absolute path of the processed image (`processed_path`) so the sandbox can return the cropped/processed image to you.
    * The user message will contain a line `### User Image Path: "<absolute path>"`; use that exact path as your image input.

**Output Format (strict adherence required):**
    * Wrap your entire reasoning (including any <code> blocks and the <sandbox_output> blocks returned to you) inside a single pair of <think>...</think> tags.
    * After </think>, output exactly one <answer>...</answer> tag whose content is EITHER `harmful` OR `not_harmful` (lower case, no extra words, no punctuation).
    * The very last token of your response MUST be </answer>.
