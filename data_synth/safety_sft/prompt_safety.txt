You are a helpful assistant.

Is this meme harmful? A meme is considered HARMFUL if it has the potential to cause distress, mental or physical harm, or material damage to an individual, organization, community, or society in general — for example through mockery, hate speech, harassment, attack, slander, dehumanization, threats, calls for exclusion, or by spreading dangerous misinformation.

Solve the problem step by step. You MUST use an external Python sandbox to inspect visual details (crop the caption, zoom into a face/logo, enhance contrast, read small overlaid text) whenever the meme is not trivially clear at first glance. The Python code will be executed by an external sandbox, and the processed image (wrapped in <sandbox_output></sandbox_output>) will be returned to aid your reasoning.

Because harmful signals in memes almost always live in **overlaid text and small visual details**, image manipulation is not a bonus — it is the default step of your reasoning.

**When you MUST call the sandbox (mandatory triggers):**
    You MUST write at least one <code> block that crops or zooms whenever ANY of the following is true (these will apply to almost every input):
      1. The meme has overlaid caption text (top banner, bottom banner, speech bubble, watermark, subtitle).
      2. The image longer side is >= 512 pixels — details will be lost at model resolution unless you crop.
      3. There is a face, gesture, symbol, flag, or logo occupying less than roughly one third of the frame.
      4. The image contains multiple panels, a comic strip, or a screenshot with UI chrome.
      5. You cannot confidently read every word of the overlaid text from the full image alone.
    You may skip the sandbox ONLY when the meme is a single low-resolution image with NO overlaid text and NO small target — this is rare. If you skip, you MUST justify the skip in one sentence inside <think>.

**How to call the sandbox (Thyme protocol — do not deviate):**
    * You may call the sandbox up to 3 times. Do NOT call it just to "look around"; each call must have a concrete goal (e.g. "read the bottom caption", "zoom into the face in the top-right").
    * Wrap every Python snippet exactly as follows (the closing </code> is what triggers execution):
    <code>
    ```python
    # your code
    ```
    </code>
    * At the end of your code, print the absolute path of the processed image (`processed_path`) so the sandbox can return the cropped/processed image to you as an <sandbox_output>...<image>...</sandbox_output> block.
    * Prefer `cv2` / `PIL` for cropping and resizing. When cropping, keep the region generous enough to include full words.
    * After you receive the sandbox image, **describe in words what you now see** (the exact text you can now read, the identity/expression of the face, etc.) before deciding your verdict.

**Output Format (strict adherence required):**
    * Wrap your ENTIRE reasoning (all <code> blocks, all <sandbox_output> blocks returned to you, and all natural-language analysis) inside a single pair of <think>...</think> tags.
    * After </think>, output exactly one <answer>...</answer> tag whose content is EITHER `harmful` OR `not_harmful` (lower case, no extra words, no punctuation).
    * The very last token of your response MUST be </answer>.

---

**CRITICAL — image path handling:**
    * The user message will always contain a line `### User Image Path: "<absolute path>"`. You MUST take that exact string and assign it to `img_path` yourself when writing code.
    * The examples below intentionally LEAVE OUT the `img_path = "..."` assignment line (it is commented out) so that you cannot copy a fake string. It is YOUR responsibility to insert that line using the real absolute path from `### User Image Path`.
    * Do NOT invent paths like `/data/foo.jpg`, `/tmp/some_meme.png`, `/data/img_123.png`, or any path you did not see verbatim in `### User Image Path`. If you cannot find `### User Image Path` in the user message, stop and do not call the sandbox.
    * Always call `cv2.imwrite(out, crop)` on the crop, THEN `print(out)`. Do not `print` a path you have not actually written to disk.
    * When you write the crop to disk, ALWAYS place it under the directory `/mnt/data/temp_processed_images/` (e.g. `out = '/mnt/data/temp_processed_images/crop_caption.jpg'`). The sandbox transparently rewrites that prefix to its own temp directory; if you save under `/tmp/` or any other location, the sandbox will NOT return your image and your reasoning will lose the crop.

**Example 1 (HARMFUL, with sandbox crop — this is the expected pattern):**

<think>
The meme shows a photo of two people with a large white bottom caption. The caption text is small at full resolution, so I will crop the bottom band to read it exactly.
<code>
```python
import cv2, os
# img_path = "..."   # <-- YOU must write this line yourself, using the exact absolute path from '### User Image Path' in the user message above. Do NOT copy this comment; replace it with a real assignment.
img = cv2.imread(img_path)
assert img is not None, f"could not read {img_path}"
h, w = img.shape[:2]
# take the bottom 35% where the caption sits
crop = img[int(h*0.65):h, 0:w]
out = '/mnt/data/temp_processed_images/crop_caption.jpg'
cv2.imwrite(out, crop)
print(out)
```
</code>
<sandbox_output>[cropped bottom banner image]</sandbox_output>
Now I can read the caption clearly: "GO BACK TO YOUR COUNTRY". Combined with the image of a visibly non-white family, this is a xenophobic slur directing exclusion at an ethnic group. That constitutes hate speech targeting a protected group, so the meme is harmful.
</think>
<answer>harmful</answer>

**Example 2 (NOT_HARMFUL, still with a sandbox crop — you must verify, not assume):**

<think>
The meme shows a cat sitting at a laptop with a small top caption I cannot fully read. I will crop the top strip to confirm the wording is not a slur or targeted attack.
<code>
```python
import cv2
# img_path = "..."   # <-- YOU must write this line yourself, using the exact absolute path from '### User Image Path' in the user message above. Do NOT copy this comment; replace it with a real assignment.
img = cv2.imread(img_path)
assert img is not None, f"could not read {img_path}"
h, w = img.shape[:2]
crop = img[0:int(h*0.25), 0:w]
out = '/mnt/data/temp_processed_images/crop_top.jpg'
cv2.imwrite(out, crop)
print(out)
```
</code>
<sandbox_output>[cropped top banner image]</sandbox_output>
The caption reads "ME PRETENDING TO WORK ON MONDAYS". This is generic self-deprecating office humour with no target group, no slur, no threat, and no misinformation. Therefore the meme is not harmful.
</think>
<answer>not_harmful</answer>

Do not copy either example verbatim — reason from the actual image you are given. In both examples above, the `img_path = "..."` line is intentionally commented out; you must ADD it back in your own code, using the exact absolute path from the `### User Image Path` line in the user message. Follow the mandatory-triggers rule above: if the input has overlaid text or is >= 512px on the long side, you must crop before you answer.
